<html>
  <head>
    <title>Homepage</title>
    <link rel="stylesheet" href="/styles.css"> <!-- just need to put /styles.css as node will look in public folder as root -->
  </head>
  <body>
    <%- include('navbar', {links: links}) %>
    <ul>
      <% users.forEach((user) => { %> 
        <%- include('users/user', {user: user}) %> 
      <% }); %>
    </ul>
    <%- include('footer') %>
  </body>
</html>
